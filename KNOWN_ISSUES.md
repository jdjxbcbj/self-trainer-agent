# Known Issues / 技术债账本

> 本文件是「问题账本」：把已经定位、但**尚未修复**（或**尚未想清楚怎么修**）的问题集中登记，
> 避免靠记忆和口头共识。每个条目都带证据（file:line）、复现、预期行为、修复方向与风险，
> 将来任何人拿到这份账本都能直接进入状态，不必重新翻代码回忆。
>
> **状态约定**：
> - `open` = 已定位、待处理
> - `deferred` = 已记录、暂缓（需先设计 / 等真实语料，见条目内的「重新评估触发条件」）
> - `fixed` = 已修复（注明 commit hash）
>
> **分类约定**（这是本账本能成立的根基）：
> - `BUG` = 确定性缺陷，与产品判断无关，修复风险低，**可以独立先修、不必等**。
> - `DESIGN` = 涉及评分/判定语义的设计缺陷，**容易矫枉过正**，需先想清楚判定逻辑、甚至先攒真实语料再动。
> - `PROCESS` = 工程流程 / 仓库结构问题，单独决策，不混进代码补丁。

---

## BUG-001 API 会话结束后仍可继续 turn，且 /end 可重复调用

- **状态**: open
- **类型**: BUG（可安全修复）
- **严重度**: 中
- **证据**:
  - `api.py:119-129` `handle_turn` 只查 `_sessions.get(session_id)`，不检查会话是否已结束。
  - `api.py:132-142` `end_session` 结束后**未**从 `_sessions` 移除 `session_id`。
  - `api.py:124` / `api.py:137` 报错文案已写「会话不存在**或已结束**」，但「已结束」分支**永远不可达**——结束的会话既不 pop、也不打标。
  - `review_agent.py:144` `practice_count = profile.get("practice_count", 0) + 1`，每次 review 都累加。
- **当前行为**:
  - 会话 `/end` 之后继续调 `/turns`，仍会成功追加回合（往已 ended 的会话里继续写）。
  - `/end` 调两次 → `practice_count` 从 0 累加到 2。
  - 与 `API.md` 承诺的「404（session_id 不存在或已结束）」矛盾。
- **预期行为**: 会话结束后，`/turns` 与 `/end` 均返回 404 且不产生副作用。
- **修复方向**: `end_session` 内 `_sessions.pop(session_id, None)`；`handle_turn` / `end_session` 增加「会话已结束 → 404」判定。
- **风险**: 低。纯生命周期逻辑，与产品判断无关，可用单测覆盖。
- **建议**: 不必等「统一改」；这是唯一一个建议**独立先修**的条目。

---

## DESIGN-001 红线检测把「合规降温句」误判成违规（一票否决）

- **状态**: deferred（需设计）
- **类型**: DESIGN（易矫枉过正，先记录后设计）
- **严重度**: 高（产品核心护城河的根基）
- **证据**:
  - `strategy_kb.py:53-74` `RED_LINES` 为纯子串匹配（`detect_red_line` 在 `108-118` 用 `k.lower() in lower`）。
  - `r-violence` 关键词含 `"打你" / "动手" / "砸" / "揍"`。
  - `judge_agent.py:44-47` 红线检测跑在维度匹配**之前**，命中即短路返回一票否决。
  - 输入「我不会动手打你」→ 同时命中 `动手` + `打你` → 判 `r-violence`，`total_score=30`。
  - 更本质的细节：`"不会"` **本身就是 g-boundary 的正向关键词**（`strategy_kb.py:25`），但因为红线先短路，系统手里攥着「正确」的关键词却永远走不到维度匹配那一步。
- **当前行为**: 安全降温句（对威胁的拒绝/守法/自保）被当作红线违规一票否决。
- **预期行为**: 能区分「真实威胁」与「对威胁的拒绝 / 降温 / 守法」。
- **修复方向（未定，记录备选）**:
  1. **首选**：先做「安全模板白名单」短路——若回应等于/包含系统自己教的 `alternative` 句子或「我不会+暴力动词」这类已教过的降温模板，**先于红线匹配**判安全。确定性、低成本，只放行系统自己教的话，不会往「放过真威胁」方向出错。
  2. **慎用**：不要上「否定词+关键词=安全」的通用规则。它分不清「我不是要**人肉**你」（该放行）和「别以为我不敢**动手**」（该抓）。对安全训练器，红线**漏判比误判更糟**。
  3. 通用否定处理等有真实语料后，再考虑规则/小模型混合，不现在造轮子。
- **风险**: 高。任何改动都会牵动红线漏判/误判的平衡，需谨慎设计。
- **重新评估触发条件**: 真实游玩语料落库后，先从 `turns` 表抽样一批「含否定词 + 红线关键词」的真实句子，统计误判率，再启动设计。

---

## DESIGN-002 评分维度二值化，且完全无视上下文

- **状态**: deferred（需设计）
- **类型**: DESIGN
- **严重度**: 中高
- **证据**:
  - `judge_agent.py:64` 维度命中关键词即 `100`，否则 `0`；`total_score` 就是命中权重之和。
  - `judge_agent.py:37-38` 明确标注 `history` / `audience` 为「预留」，实际未用。
  - `strategy_kb.py:91-106` `match_dimensions` 只做「关键词是否出现」的二值判断，不数密度、不看位置。
- **当前行为**: 评分类似「关键词 bingo」——一句恰好踩中关键词的短句能暴击，一句没踩中关键词的实质长文得 0 分。这鼓励「凑关键词」而非「真正回应」。
- **预期行为**: 至少消费 `history` 做最小上下文判断，从「判招式（关键词）」向「判目标（该处境下回应是否得当）」靠近。
- **修复方向（未定）**:
  - 短期可先让维度得分**连续化**（维度内命中关键词数 → 分档，而非 100/0），成本低、仍确定性。
  - 中期再引入 `history`：先定义「目标」的判定标准，再让 judge 消费历史（比如「本轮未踩关键词但延续了上一轮已确立的边界」不归零），避免拍脑袋加规则。
- **风险**: 中高。涉及评分体系设计，改动面大。
- **重新评估触发条件**: 与 DESIGN-001 一并，等真实语料落库后先看清真实分布再动。

---

## PROCESS-001 暴击阈值 85 是「对着手写答案」校准的

- **状态**: deferred
- **类型**: PROCESS（方法论问题，不是代码 bug）
- **严重度**: 中
- **证据**:
  - `probe_gsb.py:26-41` 语料是 `CORPUS` 里手工写死、按「优秀/良好/普通/顶撞/空洞/红线」**分好类再喂进去**的样本。
  - `probe_gsb.py:10-11` docstring 自认「当前语料是开发期手工构造的代表性样本，真实游玩数据落库后应替换为重跑」。
  - `PLAN.md §9` 决策 #4 却准备「联调后若暴击率 <15% 就把阈值 85 降到 80」——用这份手写语料去冻结判定参数，等于拿自己出的卷子给自己打分。
- **当前行为**: 判定参数的校准基准不具备代表性，真实用户输入分布会完全不同。
- **预期行为**: 判定参数用**真实游玩数据**校准。
- **修复方向**: 暂不调阈值；`probe_gsb.py` 保留，但只当**回归护栏**用（规则改完跑一遍，看有没有破坏既有打分），不作为校准源。等 `storage` 沉淀足够真实 `turns` 后，把 `CORPUS` 换成真实语料重跑，再决定降不降 85。
- **风险**: 低（不动代码，只是决策纪律）。
- **重新评估触发条件**: 真实 `turns` 达到一定量（如 ≥100 条含多样分布的回应）后，重跑 `probe_gsb.py`。

---

## DESIGN-003 「真值」散落三处内存，DB 名存实亡

- **状态**: deferred（部分可视为 bug）
- **类型**: DESIGN（源头是「谁是真值」的设计未定；其中「重启孤儿会话」是确定性后果）
- **严重度**: 中
- **证据**:
  - 会话活跃状态同时存在于 `api.py:57 _sessions`、`router.py:70-71 _crit_counts/_turn_counts`、`memory` 三处进程内内存。
  - `router.py:230` `end_session` 读暴击数读的是 `self._crit_counts.get(...)`，**不是** `storage.get_turns(...)` 重算。
  - `PLAN.md §7.2` 自己写了「router 的内存计数非权威、应从 storage.get_turns 重算」，但代码没有这个重算。
  - 后果：`api` 进程一重启，`_sessions` 清空，`trainer.db` 里却留下一堆 `status='active'` 的孤儿会话。
- **当前行为**: 计数与 DB 可能不一致；重启后 DB 残留无法收敛的 active 会话。
- **预期行为**: DB（`sessions`/`turns` 表）作为权威真值；内存计数要么从 DB 重算，要么明确「进程内会话不跨重启」并处理孤儿。
- **修复方向**: 明确真值来源——重算路径（`storage.get_turns` 数暴击/回合）或会话状态校验（`get_session` 查 status）。与 BUG-001 的「会话结束态」强相关，可一并设计。
- **风险**: 中。牵涉数据层语义，但方向清晰。
- **重新评估触发条件**: 修 BUG-001 时一并定夺真值来源。

---

## PROCESS-002 仓库三处分叉，前端/后端至今未合流

- **状态**: open
- **类型**: PROCESS（仓库策略，单独决策）
- **严重度**: 中
- **证据**:
  - `master`（后端，多提交）与 `frontend-ui`（前端，1 提交、从 initial commit 分叉）从未合并；`git log` 与 `git branch -a` 可见两分支平行。
  - 真实前端另在仓库外的 `safety-confrontation-game/`；`frontend/` 目录在 master 上被 `.gitignore` 排除。
- **当前行为**: 前端文件虽已进 git（frontend-ui 分支），但前后端仍不在一个合流点，联调靠 `PLAN.md §3.4` 那张「分叉对照表」人工对齐。
- **预期行为**: 明确合流策略——是 `master` 反合 `frontend-ui`，还是前端长期独立仓库。
- **修复方向**: 单独定一次仓库策略，别和规则修复混在一条 PR 里。
- **风险**: 低（决策成本，无代码风险）。

---

## 附：验证说明

以上 6 条的 `file:line` 与复现行为，均已在 2026-09-01 对当前 `master` 代码逐条核实
（`strategy_kb.py` / `judge_agent.py` / `api.py` / `router.py` / `review_agent.py` / `storage.py` / `probe_gsb.py`）。
若后续重构改变了行号或行为，更新本文件时请一并修正证据，避免账本过期。
