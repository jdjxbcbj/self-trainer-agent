# 安全对线训练场 · 产品需求文档（PRD v1）

> 版本：v1（在 `safe-trainer/docs/PRD-v0.md` 基础上重写）
> 状态：待评审
> 日期：2026-08-31
> 定位：回答「这个产品是什么、做给谁、核心循环与关键机制是什么、MVP 做什么」。与《PLAN.md》（后端实现规划）配套阅读——PRD 管「做什么、为什么」，PLAN 管「怎么做」。

---

## 0. 一句话定义

**安全对线训练场 = 全年龄段场景化安全教育产品：不教吵架，只教自保，把「边界感」与「安全力」练成本能。**

用户在真实冲突场景（邻里、亲戚、商家、校园、海外、暴怒对峙）里，用一个「毒舌但守法」的 AI 导师陪练，练习怎么守住边界、合法维权、情绪控场，而不是互相伤害。

---

## 1. 从 v0 到 v1：产品如何演进

v0 的起点是「软技能训练」——帮「老实人」在催婚等**关系冲突**里学会「想维持关系时不撕破脸地挡回去」。它确立了三件仍然成立的事：

1. **约束先行**：同一句冲突，因为用户处境不同，正确回应完全不同，评分不能一维化。
2. **学-练-爽三层循环**：对话只是练习场，方法论与反馈才是产品价值。
3. **判目标、不判招式**：库内策略是参考答案，不是唯一答案。

v1 做了**一次定位的升维**，动因是真实用户测试暴露的边界：

| 维度 | v0（软技能/关系维护） | v1（安全教育/自保） |
|---|---|---|
| 一句话 | 教会老实人在具体约束下守住边界 | 全年龄段，把边界感与安全力练成本能 |
| 目标用户 | 成年人（「老实人」） | 青少年 + 成年人两个身份 |
| 冲突类型 | 关系型（催婚、亲戚） | 日常 + 法律 + 涉外 + 情绪，四类模块 |
| 核心约束变量 | 关系维护意愿（4 档光谱） | 训练身份 + 对峙值 + 红线库 |
| 评分主体 | LLM 目标达成判断 | 本地规则库（GSB）+ AI 仅做 NPC |

「催婚」没有消失，它降格为「亲戚道德绑架」——众多场景中的一个。产品从「教人维护关系」升级为「教人在任何冲突里自保」，边界感是共通的底层能力，安全力是它的行动表达。

---

## 2. 目标用户

### 2.1 两类训练身份（不是两类产品）

- **青少年（未成年）**：校园冒犯、被起哄、隐私被开玩笑。核心痛点是「不知道什么时候该求助大人、怎样在不互相羞辱的前提下叫停」。所有场景标记 `minorSafe`，未成年人只进入无成人风险、不鼓励对抗的场景。
- **成年人**：邻里纠纷、商家维权、亲戚道德绑架、海外歧视、暴怒对峙。核心痛点是「想守住边界又不想把事情搞大」「事后越想越亏、当时说不出话」。

### 2.2 共同痛点（沿用 v0 且被验证）

- 触发时刻不是「冲突前」，而是**冲突后的后悔时刻**（「我当时应该那么说」）。
- 替代品是刷博主短视频学方法论，痛点是**太教条、没耐心、看了用不出来**（博主的处境和用户本人不一致）。

---

## 3. 定位

**从「对话模拟器」升级为「场景化安全教育产品」——学、练、爽三层循环。**

| 层 | 作用 | 落地 |
|---|---|---|
| 学 | 进场景前/中，给一句「合规提示」（招式：什么时候用/怎么用/为什么） | 教学提示 + 安全导师「老边」 |
| 练 | 场景实战，带着身份、对峙值、红线约束 | 6 场景 × 4 模块 |
| 爽 | 通关暴击、连击、等级、徽章、下一个 boss | XP / 等级 / 徽章系统 |

三者不是三个模式，是**一个循环的三个截面**。

---

## 4. 核心循环（产品主路径）

```
① 选身份（青少年/成年人）
   → ② 选场景关卡（按模块与风险分级）
   → ③ 学：安全导师「老边」给一句合规提示
   → ④ 练：用户回应（打字 / 语音）
   → ⑤ 评分：本地规则库判「这句回应对不对、是否触碰红线」
   → ⑥ 爽感反馈（暴击/连击/升级） + 复盘报告
   → 通关 / 重开，进入下一个更难的 boss
```

关键点：③ 是「学」，⑤ 是「判」。策略建议在**回合前/中**给，评分在**回合后**给——避免回到「抄标准答案」的老路。

---

## 5. 关键机制

### 5.1 自研库约束（最高优先级，产品护城河的核心）

**AI 只做场景 NPC；评分、通关、违规拦截均由本地规则库决定。**

| 库 | 名称 | 内容 | 职责 |
|---|---|---|---|
| SDB | 场景库 | 场景、人设、开场白、法条、评价维度、话术分级 | 生成关卡 |
| GSB | 评分库 | 正向/负向关键词、维度权重、暴击线 | 回合打分 |
| RSB | 红线库 | 辱骂 / 暴力威胁 / 违法维权 / 歧视性反击 | 违规拦截，一票失分 |

这条原则是对 v0「判目标、不判招式」的工程落地：**规则可预测、零成本、可调试、不依赖模型正确性**。LLM 的能力只用在「把 NPC 演得自然」上，不承担「评判」责任——评判交给确定性规则。

### 5.2 对峙值（核心张力指标）

单局内从 50 起跳的动态压力条。用户回应越合规、越降温，对峙值下降；越顶撞、越空洞，对峙值上升。它是「这一局有没有在恶化」的可视化反馈，也是触发暴击/通关的参考信号。

### 5.3 评分维度（GSB）

七维正向 + 两维负向：

- **正向**：表达边界、保持冷静、合规合法、降温控场、取证意识、礼貌、求助意识
- **负向**：指责对抗、空洞敷衍（反向扣分）

评分给的是**分维度反馈**，而不是「+5 普通回应 + 同一句文案」。暴击 = 边界清晰、情绪稳定、合规合法、止损控场、有取证或求助意识，多维度同时命中。

### 5.4 红线库（RSB，一票否决）

四类违规，命中即判定违规失分，并给出「合法替代句」：

1. **辱骂**（滚/废物/傻/垃圾…）→ 把安全训练变成互撕
2. **暴力威胁**（打你/弄死/动手…）→ 直接违规
3. **违法维权**（曝光身份证/人肉/堵门…）→ 从受害方变风险方
4. **歧视性反击**（涉外场景）→ 放大风险

红线的价值不只是「扣分」，而是**教「更安全的那句话」**——每个红线都配一句合规替代。

### 5.5 教学提示（继承 v0 的教学卡）

v0 的教学卡（招式/什么时候用/怎么用/为什么）收敛为**「一句合规提示」**：进场景先给一句可抄的边界表达，回合中按需再给一句。目标不变——30 秒可看完、看完能用。

### 5.6 爽感循环（复用雏形，接入循环）

暴击特效 → 连击累积 → 升级 → 徽章 → 解锁下一个 boss（场景）。爽感是「练得下去」的燃料，不是独立模式。

### 5.7 安全导师「老边」（人格化 NPC）

「坏话我来扛，边界你来守。」——**毒舌但守法，嘴硬但不教互撕**。他是产品的人格化身：让「学自保」这件事不无聊、不道貌岸然，同时用嘴上的分寸示范「有边界不等于没教养」。

---

## 6. 系统架构（与 PLAN.md 衔接）

后端是**纯 Python 多 Agent 系统**，各 Agent 分工如下（详见《PLAN.md》）：

| 层 | 组件 | 职责 |
|---|---|---|
| 编排 | `router.py` + `main.py` | 对话阶段状态机 + Agent 调度 + 总装配 |
| Agent | `roleplay_agent.py` | 生成 NPC 回应 + 下一轮状态（唯一用 AI 演戏的地方） |
| Agent | `judge_agent.py` | 回合级打分 + 反馈（消费 GSB 规则） |
| Agent | `teaching_agent.py` | 教学提示（规则/缓存优先） |
| Agent | `review_agent.py` | 会话结束复盘 + 更新用户画像 |
| 支撑 | `scenario_store.py` / `strategy_kb.py` / `knowledge_base.py` | SDB / GSB / 内容层 |
| 支撑 | `memory.py` / `profile.py` | 短时记忆 / 长时画像（SQLite） |

> ⚠️ **一处待对齐**：前端「AI 只做 NPC、评分由本地规则库决定」与 PLAN.md 中 `judge_agent` 用 LLM 打分的描述存在张力。PRD 的立场是**规则库优先**（GSB 打分 + RSB 拦红线，LLM 只做 NPC）；落地时评分是否引入 LLM 兜底，需在实现时定夺，但「评判权不交给模型」是产品原则。

---

## 7. MVP 范围

**6 个场景 × 1 个循环，跑通即胜利。**

- 场景：邻里噪音、亲戚道德绑架、商家退款、校园冒犯、海外种族挑衅、暴怒对峙（已在前端 demo 成形）。
- 循环：选身份 → 选场景 → 合规提示 → 实战回应 → 规则库评分 → 爽感 + 复盘。
- 明确不做（本期）：多约束变量叠加、场景动态生成、全网方法论整合、真实语音识别（先做前端语音输入占位）。

---

## 8. 已知限制与后续

| 限制 | 说明 | 何时处理 |
|---|---|---|
| 评分规则是关键词匹配 | 语义理解依赖 LLM 兜底，当前 GSB 是正则/关键词 | 数据量起来后引入目标达成度判断 |
| 单对手场景 | 真实冲突常演变为多人施压（批斗大会） | 单对单跑通后扩展群体施压 |
| 场景固定 6 个 | 数据沉淀后按模块复制扩展 | 循环稳定后 |
| 未成年/成人共用一套评分 | 两者「正确回应」标准不同，尚未分流 | 身份切换先做场景隔离，再做评分分流 |
| 后端与前端尚未联调 | 后端 PLAN 基于催婚场景，前端已切安全场景 | 见 §6 待对齐项 |

---

## 9. 护城河说明

**护城河 = 自研库（SDB/GSB/RSB）+ 让方法练得起来的产品。模型能力是商品，规则库与场景化训练闭环才是资产。**

- 博主视频是原料，谁都能拿；你独有的价值是：**什么场景、用什么话、为什么（规则库方法论）+ 身份约束（识别用户处境）+ 交互反馈（练得起来）**。
- 裸模型给不了这套——DeepSeek 能聊天，但不能替一个「老实人」判断「这句回应对不对、是否踩线、更安全的那句是什么」。

**一句话：安全对线训练场卖的不是「会对话」，是「教人分清自保与互撕的边界，并在具体冲突里把自保练成本能」。**
